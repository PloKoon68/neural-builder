reduce the equarions to one line
- do the matrix (for loop) operations on loops instead of replicating.
- If sigmoid and binary cross entropy used together on last layer, ruce to dL/dZ (a - y)
- Same for softmax



değiştir kesinlikle: 
- git bütün activation ların derivative lerini z ye göre yap hatta a yı çıkar kütüphaneden
- mesela softmax derivativinde gidiyorsun boş boş kopyalıyorsun onu predictions kullan direk.
- gereksiz incude ları kontrol et mesela loss factory.cpp de tekrar lossfucntion import etmeye gerek var mı çünkü zaten lossfactory.h de etmişsin.
- mesela accuracy hesaplamada arraying boyurunu hesaplamaya çalışmaktansa git içindeki elemanlarda dön.


yapılması gerekenler:
- son layera sınır koy veri setinle uyuşuyor mu diye mesela kalkıp da softmax için 10 tane classın var da senin final layerın 5 tane neuronsa olmaz uyuşmazlık olur.
- eğer ki compile ederken get weights and biasler çok uzun sürerse, ilk oluşturulurken direk kaydet.



comparisions:
crow json   -   rapidjson,simdjson